## Music Backend (Node.js + Express + MongoDB)

### Scripts
- `npm run dev`: start in watch mode
- `npm start`: start server

### Env
Copy `.env.example` to `.env` and fill values.

### Endpoints
- Health: `GET /health`
- User routes under: `/api/v1/user`
- Admin routes under: `/api/v1/admin`


